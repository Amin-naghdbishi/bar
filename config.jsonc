{
  "layer": "top",
  "position": "bottom",
  "height": 38,
  "spacing": 0,
  "margin-top": 0,
  "margin-bottom": 0,
  "margin-left": 0,
  "margin-right": 0,

  // Layout: Workspace -> Active Window -> Applications -> System Information
  "modules-left": [
    "niri/workspaces",
    "niri/window"
  ],

  "modules-center": [],

  "modules-right": [
    "wlr/taskbar",
    "network",
    "clock",
    "niri/language",
    "disk",
    "custom/home",
    "memory",
    "battery",
    "pulseaudio",
    "pulseaudio#microphone"
  ],

  // ---------------------------------------------------------------------------
  // Left Side: Current Workspace (Number Only)
  // ---------------------------------------------------------------------------
  "niri/workspaces": {
    "current-only": true,
    "format": "{index}",
    "tooltip": false
  },

  // ---------------------------------------------------------------------------
  // Left Side: Active Focused Window Title (Text Only)
  // ---------------------------------------------------------------------------
  "niri/window": {
    "format": "{}",
    "icon": false,
    "max-length": 50,
    "separate-outputs": false
  },

  // ---------------------------------------------------------------------------
  // Middle/Right Side: Taskbar Open Applications (Cinnamon-inspired)
  // ---------------------------------------------------------------------------
  "wlr/taskbar": {
    "format": "{icon}",
    "icon-size": 18,
    "tooltip-format": "{title}",
    "on-click": "activate",
    "on-click-middle": "close",
    "sort-by-app-id": true
  },

  // ---------------------------------------------------------------------------
  // System Information in exact order:
  // Wi-Fi -> Clock -> Language -> Disk -> Home -> RAM -> Battery -> Volume -> Mic
  // Each module displays BOTH its icon and its value with consistent spacing
  // ---------------------------------------------------------------------------

  // 1. Wi-Fi
  "network": {
    "interval": 5,
    "format-wifi": " {signalStrength}%",
    "format-ethernet": "󰈀 Eth",
    "format-disconnected": "󰤮 Off",
    "format-disabled": "󰤮 Off",
    "tooltip-format": "Wi-Fi: {essid} ({signalStrength}%)\nIP: {ipaddr}"
  },

  // 2. Clock (Time: HH:MM)
  "clock": {
    "interval": 1,
    "format": " {:%H:%M}",
    "tooltip-format": "{:%A, %B %d, %Y}"
  },

  // 3. Keyboard Layout (e.g. EN, DE)
  "niri/language": {
    "format": " {}",
    "format-en": "EN",
    "format-us": "EN",
    "format-de": "DE",
    "format-fa": "FA",
    "format-ir": "FA",
    "format-fr": "FR",
    "format-es": "ES",
    "format-it": "IT",
    "format-ru": "RU",
    "format-tr": "TR",
    "format-uk": "UK",
    "format-ar": "AR",
    "format-pt": "PT",
    "format-nl": "NL",
    "format-pl": "PL"
  },

  // 4. Free Disk Space (e.g. 124G)
  "disk": {
    "interval": 30,
    "path": "/",
    "format": " {specific_free:0.0f}G",
    "unit": "GB",
    "tooltip-format": "Disk: {free} free ({percentage_free}%)"
  },

  // 5. Home Launcher (Icon and label)
  "custom/home": {
    "format": " Home",
    "tooltip": false,
    "on-click": "xdg-open ~"
  },

  // 6. RAM Usage (e.g. 6.2G)
  "memory": {
    "interval": 2,
    "format": " {used:0.1f}G",
    "tooltip-format": "RAM: {used:0.1f}GiB / {total:0.1f}GiB ({percentage}%)"
  },

  // 7. Battery (Icon and percentage, e.g. 87%)
  "battery": {
    "states": {
      "warning": 30,
      "critical": 15
    },
    "format": "{icon} {capacity}%",
    "format-charging": " {capacity}%",
    "format-plugged": " {capacity}%",
    "format-icons": ["", "", "", "", ""],
    "tooltip-format": "Battery: {capacity}%\n{timeTo}"
  },

  // 8. Volume (Speaker icon and percentage, e.g. 75%)
  "pulseaudio": {
    "format": "{icon} {volume}%",
    "format-muted": " Muted",
    "format-icons": {
      "headphone": "",
      "hands-free": "",
      "headset": "",
      "default": ["", "", ""]
    },
    "tooltip-format": "Volume: {volume}%\nDevice: {desc}",
    "on-click": "pactl set-sink-mute @DEFAULT_SINK@ toggle",
    "on-click-right": "pavucontrol",
    "on-scroll-up": "pactl set-sink-volume @DEFAULT_SINK@ +5%",
    "on-scroll-down": "pactl set-sink-volume @DEFAULT_SINK@ -5%"
  },

  // 9. Microphone (Microphone icon and level/status)
  "pulseaudio#microphone": {
    "format": "{format_source}",
    "format-source": " {source_volume}%",
    "format-source-muted": " Muted",
    "tooltip-format": "Microphone: {source_volume}%\nDevice: {source_desc}",
    "on-click": "pactl set-source-mute @DEFAULT_SOURCE@ toggle",
    "on-scroll-up": "pactl set-source-volume @DEFAULT_SOURCE@ +5%",
    "on-scroll-down": "pactl set-source-volume @DEFAULT_SOURCE@ -5%"
  }
}
